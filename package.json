{
  "name": "railway_interview_project",
  "version": "1.0.0",
  "description": "interview project for railway senior full stack engineer - product",
  "main": "src/server.ts",
  "scripts": {
    "start": "npm run prd",
    "prd": "npm run compile && set DOTENV_CONFIG_PATH=.env && node build/src/server.js",
    "dev": "set DOTENV_CONFIG_PATH=.env.development.local && nodemon src/server.ts",
    "test": "jest",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/JosephLYH/railway_interview_project"
  },
  "author": "Lam Yuen Ho Joseph",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "20.11.5",
    "gts": "^5.3.0",
    "jest": "^29.7.0",
    "nodemon": "^3.1.0",
    "typescript": "~5.4.3"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ts-node": "^10.9.2"
  }
}
